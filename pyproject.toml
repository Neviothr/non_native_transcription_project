[project]
name = "non-native-transcription-workbench"
version = "1.6.2"
description = "Machine-assisted transcription of non-native English conversations with error and disfluency preservation"
requires-python = "==3.14.6"
dependencies = [
    "pywhispercpp==1.5.0",
    "imageio-ffmpeg==0.6.0",
]

[tool.unittest]
start-directory = "tests"
